name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout files
        uses: actions/checkout@v1

      - name: Set version
        run: |
          cat $(date +%y.%m.%d%H%M) > VERSION

      - name:  Echo
        run: |
          echo "$(cat VERSION)"

      - name: Build & publish
        run: |
          dotnet publish src/MyWeb --output .publish/MyWeb

      - name: Zip
        run: |
          zip -r .publish/MyWeb-$(cat VERSION).zip .publish/MyWeb

      - name: Create release
        id: create_release
        uses: actions/create-release@v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v.$(cat VERSION)
          release_name: v.$(cat VERSION)
          draft: false
          prerelease: false

      - name: Upload assets
        uses: actions/upload-release-asset@v1.0.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: .publish/MyWeb-$(cat VERSION).zip
          asset_name: MyWeb-$(cat VERSION).zip
          asset_content_type: application/zip
